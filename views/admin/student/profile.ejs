<%- include('../layoutes/header.ejs') %>
    <%- include('../layoutes/sidebar.ejs') %>

        <!-- Content Wrapper. Contains page content -->
        <div class="content-wrapper bg-warning ">
            <!-- Content Header (Page header) -->
            <div class="content-header">

            </div>
            <!-- /.content-header -->
            <!-- Main content -->
            <!-- /.card -->

            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Update Profile</h3>
                    </button>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <div class="modal-body">
                        <form action="/updateprofile" method="post" enctype="multipart/form-data">

                            <div class="form-group">
                                <label>Name</label>
                                <input type="text" name="name" value="<%= n %>" class="form-control" required>
                            </div>
                            <div class="form-group">
                                <label>Email</label>
                                <input type="email" name="email" value="<%= e %>" class="form-control"required>
                            </div>
                            <div class="form-group">
                                <label>Phone</label>
                                <input type="number" name="phone" value="<%= p %>" class="form-control"required>
                            </div>
                            <div class="form-group">
                                <label>City</label>
                                <input type="text" name="city" value="<%= c %>" class="form-control"required>
                            </div>
                            <div class="form-group">
                                <label>Address</label>
                                <textarea name="address" class="form-control"required><%= a %></textarea>
                            </div>
                            <div class="form-group">
                                <label>Image</label>
                                <input type="file" value="<%= image.image %>" name="image" class="form-control" required>
                              </div> <br>
                              <img src="<%= image.url %>" alt="" width="50" style="border-radius: 20px;">

                            <button class="btn btn-success" type="submit"> Update Profile</button>
                        </form>
                    </div>

                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->

            <!-- Modal add student -->

            <!-- modal add student end -->

            <!-- /.content -->
        </div>
        <!-- /.content-wrapper -->


        <%- include('../layoutes/footer.ejs') %>